spring:
  application:
    name: recycle
  h2:
    console:
      path: /h2
      enabled: true
  datasource:
    url: jdbc:h2:mem:~/test
    username: sa
    password:
    driverClassName: org.h2.Driver